# Jot

Jot は 0, 1 のビット列でラムダ式を構成する記法。
すべてのビット列は構文エラーなく、ラムダ式に変換できる

## ラムダ式への変換ルール

`E` は任意のラムダ式。
`[E]` は `E` を Jot で表したもの。

- `(空)` = `I`
- `[E]0` = `(E S) K`
- `[E]1` = `S (K E)`

## わ

```plain
E0101 S (K (E S K (S K)))

S (K S) ((S (K (S (K S)))) (S (K K)))
```
