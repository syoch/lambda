from "basics.lambda" import I, K, S
from "boolean.lambda" import True, False
from "natural_number.lambda" import Nat0, Nat1, Nat2, Nat3, Nat4, NatIsZero

o1 = \a. \b. a
o2 = \a. \b. b

z = \sel. sel False False
f = \x. x
g = \x. x


A = Nat3 f z

# reduce_steps Nat0 g A

assert: (Nat0 g A) o2 == False
assert: (Nat1 g A) o2 == False
assert: (Nat2 g A) o2 == False
assert: (Nat3 g A) o2 == True
assert: (Nat4 g A) o2 == False
